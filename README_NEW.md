# Bybit Breakout Scanner Bot

Умный бот для поиска breakout сигналов на Bybit SPOT.

## 🎯 Концепция

Бот **НЕ покупает автоматически**. Он:
- Сканирует рынок на дешёвые альткоины (0.0005 - 1.0 USDT)
- Ищет паттерн "накопление → пробой"
- Присылает сигнал с расчётами
- Ты принимаешь решение: [Купить] или [Пропустить]

**Ты — финальный фильтр!**

## 🚀 Быстрый старт

### 1. Настройка

```bash
# Копируем пример конфига
cp .env.example .env

# Заполняем .env файл:
# - BYBIT_API_KEY и BYBIT_SECRET (https://www.bybit.com/app/user/api-management)
# - TELEGRAM_TOKEN (через @BotFather)
# - TELEGRAM_CHAT_ID (через @userinfobot)
```

### 2. Запуск через Docker

```bash
# Запуск
docker-compose up -d

# Логи
docker-compose logs -f bot

# Остановка
docker-compose down
```

### 3. Локальный запуск (для разработки)

```bash
# Создаём виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем PostgreSQL (нужен Docker)
docker-compose up -d postgres

# Запускаем бота
python main_new.py
```

## 📊 Условия сигнала

### A. Фаза накопления (обязательна)
- Диапазон цены ≤ 2.5 × ATR за 20 свечей
- EMA7/14/28 сплетены
- EMA100 плоская
- Объём ниже среднего

### B. Импульс (breakout)
- Пробой High за 20 свечей
- Закрытие выше EMA100
- EMA7 пересекает EMA14 снизу вверх
- Объём ≥ 2 × среднего

### C. Фильтр качества
- Рост свечи 0.5% - 8%
- RSI ≤ 70
- Спред ≤ 0.6%
- Стакан сбалансирован

## 🛡 Защиты

- **Анти-FOMO**: 1 сигнал на пару в 6 часов
- **Лимит позиций**: макс 3 одновременно
- **Пауза при падении BTC**: -1.5% за час → молчим
- **Стопы за день**: 2 стопа → пауза

## 📱 Команды Telegram

- `/start` - Главное меню
- `/status` - Статус бота
- `/positions` - Открытые позиции

## 🔧 Структура проекта

```
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile             # Сборка образа
├── main_new.py            # Точка входа
├── requirements.txt       # Зависимости
├── .env.example           # Пример конфига
└── src/
    ├── config.py          # Все настройки
    ├── database/          # PostgreSQL модели
    │   ├── connection.py
    │   └── models.py
    ├── exchange/          # Работа с Bybit
    │   ├── exchange.py
    │   ├── indicators.py
    │   └── scanner.py
    ├── telegram/          # Telegram бот
    │   ├── bot.py
    │   └── handlers.py
    └── trading/           # Исполнение ордеров
        ├── executor.py
        └── position_manager.py
```

## ⚙️ Настройки (src/config.py)

Все параметры можно менять в `src/config.py`:

- Фильтры активов (цена, объём)
- Условия сигнала
- Риск-менеджмент (размер позиции, SL/TP)
- Анти-FOMO параметры

## ⚠️ Дисклеймер

Торговля криптовалютами сопряжена с высоким риском. Бот не гарантирует прибыль. Используйте только те средства, которые готовы потерять.
